#include<iostream>
#include<cstring>
using namespace std;

int main() {
	string s; cin >> s;
	long long q, t, k; cin >> q;
	for (int i = 0; i < q; ++i) {
		cin >> t >> k;
		long long pos = k / t;
		
	}

	return 0;
}